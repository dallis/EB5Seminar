xml.instruct!
 
xml.urlset "xmlns" => "http://www.google.com/schemas/sitemap/0.84" do
  
  xml.url do
    xml.loc         "http://intvencap.com/"
    xml.lastmod     w3c_date(Time.now)
    xml.changefreq  "always"
  end

  xml.url do
    xml.loc         "http://intvencap.com/agenda"
    xml.lastmod     w3c_date(Time.now)
    xml.changefreq  "always"
  end

  xml.url do
    xml.loc         "http://intvencap.com/speakers"
    xml.lastmod     w3c_date(Time.now)
    xml.changefreq  "always"
  end

  xml.url do
    xml.loc         "http://intvencap.com/venue"
    xml.lastmod     w3c_date(Time.now)
    xml.changefreq  "always"
  end

  xml.url do
    xml.loc         "http://intvencap.com/sponsors"
    xml.lastmod     w3c_date(Time.now)
    xml.changefreq  "always"
  end
    xml.url do
    xml.loc         "http://intvencap.com/resources"
    xml.lastmod     w3c_date(Time.now)
    xml.changefreq  "always"
  end

  xml.url do
    xml.loc         "http://intvencap.com/contact"
    xml.lastmod     w3c_date(Time.now)
    xml.changefreq  "always"
  end

end
 