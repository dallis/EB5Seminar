%h1 Listing documents

%table.zebra
  %tr
    %th Title
    %th Attachments
    %th{:colspan => "3", :style => "text-align:center;"} Actions

  - @documents.each do |document|
    %tr
      %td= document.title
      %td= document.attachments.count
      %td= link_to 'Show', [:admin, document]
      %td= link_to 'Edit', [:edit, :admin, document]
      %td= link_to 'Destroy', [:admin, document], :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Document', [:new, :admin, :document]
