%h1 Listing sponsors

%table.zebra
  %tr
    %th Name
    %th Logo
    %th Description
    %th Website
    %th Priority
    %th
    %th
    %th

  - @sponsors.each do |sponsor|
    %tr
      %td= sponsor.name
      %td= link_to "logo",sponsor.logo.url
      %td= truncate(sponsor.description)
      %td= sponsor.website
      %td= sponsor.priority
      %td= link_to 'Show', [:admin, sponsor]
      %td= link_to 'Edit', [:edit, :admin, sponsor]
      %td= link_to 'Destroy', [:admin, sponsor], :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Sponsor', [:new, :admin, :sponsor]